# Leftover city × condition plates — snapshot 2026-08-29

These are dining-room leftovers, not first-class kitchen food.

They are WordPress city × condition pages plated by `npcwoods-wp-singular-plates.php`. They have a title and H1. They are not the ~60 KB city templates.

- Do not link them from `/sitemap/`, `/conditions/`, or state hubs.
- Do not deploy them with `deploy.py`.
- Do not hand-edit them. If a city deserves a real plate, cook it from the existing city template system.
- This folder is a freezer snapshot so git has a copy of what the dining room is serving today.

See `scripts/server-sync/LIVE-HTML-INVENTORY-2026-08-29.md`.
